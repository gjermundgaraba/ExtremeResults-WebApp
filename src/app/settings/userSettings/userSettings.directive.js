(function () {
    'use strict';

    angular
        .module('xr.settings')
        .component('xrUserSettings', {
            templateUrl: 'settings/userSettings/userSettings.partial.html'
        });

})();